# ===========================================
# BentoPortfolio - Environment Variables
# ===========================================

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Set DB_SOURCE to choose your database:
#   "docker" - Local Docker PostgreSQL
#   "remote" - Bangladesh Server
#   "render" - Render.com Cloud PostgreSQL
DB_SOURCE="render"

# Docker Database (Local) - Used when DB_SOURCE="docker"
DATABASE_URL_DOCKER="postgresql://postgres:bento_dev_password@localhost:5434/bento_db?schema=public"

# Remote Database (Bangladesh) - Used when DB_SOURCE="remote"
DATABASE_URL_REMOTE="postgresql://username:password@your-server-ip:5432/bento_db?schema=public"

# Render.com Database (Cloud) - Used when DB_SOURCE="render"
# Get this from Render Dashboard > Your PostgreSQL > External Database URL
DATABASE_URL_RENDER="postgresql://user:password@your-host.render.com:5432/dbname"

# For Prisma CLI: Set this to match your DB_SOURCE selection
DATABASE_URL="postgresql://user:password@your-host.render.com:5432/dbname"

# ===========================================
# AUTHENTICATION
# ===========================================
# NextAuth.js Configuration
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ===========================================
# EXTERNAL APIs
# ===========================================
# Steam API Key (https://steamcommunity.com/dev/apikey)
STEAM_API_KEY="your-steam-api-key"

# Google Maps API Key (https://console.cloud.google.com/apis/credentials)
# This one is public (starts with NEXT_PUBLIC_)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"
