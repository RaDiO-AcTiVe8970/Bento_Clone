# ===========================================
# Bento Clone - Environment Variables
# ===========================================

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Set DB_USE_DOCKER to "true" to use local Docker database
# Set DB_USE_DOCKER to "false" to use remote server database
DB_USE_DOCKER="false"

# Docker Database (Local) - Used when DB_USE_DOCKER="true"
DATABASE_URL_DOCKER="postgresql://postgres:bento_dev_password@localhost:5432/bento_db?schema=public"

# Remote Database - Used when DB_USE_DOCKER="false"
# Replace with your server details:
DATABASE_URL_REMOTE="postgresql://username:password@your-server-ip:5432/bento_db?schema=public"

# NextAuth.js Configuration
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Steam API Key (https://steamcommunity.com/dev/apikey)
STEAM_API_KEY="STEAM_API_KEY_HERE"

# Google Maps API Key (https://console.cloud.google.com/apis/credentials)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="NEXT_PUBLIC_GOOGLE_MAPS_API_KEY_HERE"

# Optional: Vercel specific
# VERCEL_URL is auto-set by Vercel during deployment

# For Prisma CLI: set DATABASE_URL to the one you want to use before running migrations or db push
# Example:
#   export DATABASE_URL=$DATABASE_URL_DOCKER   # for local Docker
#   export DATABASE_URL=$DATABASE_URL_REMOTE   # for remote server
# Or use the helper script: ./set-db-url.sh [docker|remote]
DATABASE_URL=""
